# Core (lightweight)
requests>=2.31.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Async
aiohttp>=3.9.0

# Report generation
jinja2>=3.1.0

# Config
pyyaml>=6.0

# Scheduling
schedule>=1.2.0

# Logging
loguru>=0.7.0

# Production server
gunicorn>=22.0

# NOTE: pandas, scikit-learn, jieba are optional for keyword matching.
# The app works without them (falls back to regex-based matching).
